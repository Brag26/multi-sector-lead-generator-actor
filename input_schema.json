{
  "title": "Multi-Sector Lead Generator Input",
  "description": "Generate business leads for different sectors using a single Apify actor.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "sector": {
      "title": "Sector",
      "description": "Select the industry sector for which leads should be generated.",
      "type": "string",
      "editor": "select",
      "enum": [
        "Healthcare",
        "Real Estate",
        "Manufacturing",
        "IT & Startups",
        "Education"
      ]
    },
    "city": {
      "title": "City",
      "description": "Target city or location for lead generation.",
      "type": "string",
      "editor": "textfield"
    },
    "postcode": {
      "title": "Postcode/ZIP Code",
      "description": "Optional postcode or ZIP code for more precise location targeting (e.g., 600001, 110001).",
      "type": "string",
      "editor": "textfield"
    },
    "keyword": {
      "title": "Keyword",
      "description": "Optional keyword to refine the type of leads (e.g., Dermatologist, Builder).",
      "type": "string",
      "editor": "textfield"
    },
    "maxResults": {
      "title": "Maximum Results",
      "description": "Maximum number of leads to generate.",
      "type": "integer",
      "editor": "number",
      "default": 10
    }
  },
  "required": ["sector", "city"]
}
